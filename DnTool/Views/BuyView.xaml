<UserControl x:Class="DnTool.Views.BuyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ComboBox x:Name="things" HorizontalAlignment="Left" Margin="79,36,0,0" VerticalAlignment="Top" Width="120" ItemsSource="{Binding Things}" DisplayMemberPath="Name"/>
        <Button Content="购买" Command="{Binding BuyCommand}" CommandParameter="{Binding SelectedItem,ElementName=things}" Click="Button_Click" HorizontalAlignment="Left" Margin="79,134,0,0" VerticalAlignment="Top" Width="75"/>
        <TextBox HorizontalAlignment="Left" Height="23" Margin="79,87,0,0" TextWrapping="Wrap" Text="{Binding Number}" VerticalAlignment="Top" Width="120"/>
        <Label Content="购买物品:" HorizontalAlignment="Left" Margin="10,36,0,0" VerticalAlignment="Top"/>
        <Label Content="购买数量:" HorizontalAlignment="Left" Margin="10,87,0,0" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
